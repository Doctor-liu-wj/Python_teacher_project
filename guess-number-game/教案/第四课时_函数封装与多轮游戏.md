# 第四课时：函数封装与多轮游戏

## 学习目标
- 理解函数的概念和作用
- 学会定义和调用函数
- 使用函数封装游戏逻辑
- 实现完整的多轮游戏

## 课程内容

### 1. 什么是函数？

函数是一段可以重复使用的代码块。它有以下优点：
- 代码复用：避免重复编写相同的代码
- 代码组织：将复杂的程序分解为简单的部分
- 代码维护：修改时只需修改一个地方

### 2. 定义函数

```python
def 函数名(参数1, 参数2, ...):
    """函数说明文档"""
    函数体
    return 返回值
```

**示例：**
```python
def add(a, b):
    """计算两个数的和"""
    return a + b

result = add(3, 5)  # 调用函数
print(result)  # 输出：8
```

### 3. 函数的参数和返回值

**参数**：函数接收的输入数据
**返回值**：函数输出的结果

```python
def greet(name):
    """问候用户"""
    message = f"你好，{name}！"
    return message

greeting = greet("小明")
print(greeting)  # 输出：你好，小明！
```

### 4. 函数的作用域

变量的作用域决定了它在哪里可以被访问：

```python
x = 10  # 全局变量

def my_function():
    y = 20  # 局部变量
    print(x)  # 可以访问全局变量
    print(y)  # 可以访问局部变量

my_function()
# print(y)  # 错误！y在函数外不存在
```

## 游戏的函数化结构

### play_game() - 单轮游戏函数

```python
def play_game():
    """进行一轮游戏"""
    target_number = random.randint(1, 100)
    attempts = 0
    max_attempts = 10
    
    # 游戏逻辑...
    
    return True  # 如果赢了返回True，否则返回False
```

### main() - 主函数

```python
def main():
    """主程序：处理多轮游戏"""
    while True:
        play_game()  # 进行一轮游戏
        
        choice = input("想再玩一次吗？(是/否): ")
        if choice == "否":
            break
```

## 完整的游戏程序

```python
import random

def play_game():
    """
    进行一轮游戏
    返回：True表示赢了，False表示失败
    """
    target_number = random.randint(1, 100)
    attempts = 0
    max_attempts = 10
    
    print("欢迎来到猜数字游戏！")
    print(f"我想了一个1-100之间的数字，你有{max_attempts}次机会\n")
    
    while attempts < max_attempts:
        try:
            guess = int(input("请输入你的猜测（1-100）: "))
            
            if guess < 1 or guess > 100:
                print("请输入1-100之间的数字！\n")
                continue
            
            attempts += 1
            
            if guess < target_number:
                print(f"太小了！剩余{max_attempts - attempts}次\n")
            elif guess > target_number:
                print(f"太大了！剩余{max_attempts - attempts}次\n")
            else:
                print(f"恭喜你！用{attempts}次猜对了！")
                return True
        
        except ValueError:
            print("请输入一个有效的整数！\n")
    
    print(f"很遗憾，你没有猜对。正确答案是{target_number}")
    return False

def main():
    """主函数：控制游戏流程"""
    print("="*50)
    print("       欢迎来到猜数字游戏")
    print("="*50 + "\n")
    
    while True:
        play_game()
        
        while True:
            choice = input("\n想再玩一次吗？(是/否): ").strip().lower()
            if choice in ['是', 'y', 'yes']:
                print("\n" + "="*50 + "\n")
                break
            elif choice in ['否', 'n', 'no']:
                print("\n感谢游玩！再见！")
                return
            else:
                print("请输入'是'或'否'！")

# 程序入口
if __name__ == "__main__":
    main()
```

## 函数最佳实践

1. **单一职责**：一个函数做一件事
2. **清晰命名**：函数名清晰表达其作用
3. **添加文档**：使用文档字符串说明函数功能
4. **避免全局变量**：尽量使用参数和返回值
5. **合理长度**：函数不宜过长（一般不超过50行）

## 课堂练习

1. 将游戏代码分解为多个函数
2. 添加记录玩家成绩的功能
3. 实现多轮游戏中的胜负统计

## 代码组织建议

```
guess_number.py
├── 导入模块
├── play_game() 函数
├── main() 函数
└── if __name__ == "__main__": 程序入口
```

## 小结
通过这节课，我们学会了：
- ✓ 定义和调用函数
- ✓ 函数的参数和返回值
- ✓ 函数的作用域
- ✓ 将复杂程序分解为多个简单函数
- ✓ 编写可维护和可复用的代码
